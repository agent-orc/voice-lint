import catalogue from '../packages/writing-rules/src/catalogue.json' with {type:'json'};
import patterns from '../packages/writing-rules/src/surface-patterns.json' with {type:'json'};
import {escape as e} from './template.mjs';
const ruleLink=(id,locale)=>'<a href="../writing-patterns/#'+locale+'-'+e(id)+'"><code>'+e(id)+'</code></a>';
const refs=(ids,locale)=>ids.map(id=>ruleLink(id,locale)).join(', ');
export function researchApplication(group,locale='en'){
 const de=locale==='de',link=ids=>refs(ids,de?'de':'en');
 const content=de?{
  practice:{title:'Regeln aus diesen Beobachtungen',body:'Ungefragte Deutungen fließen in '+link(['meta-framing','reader-goal'])+' ein. Formelhaft eingesetzte Gegensätze und wiederholte rhetorische Strukturen betreffen '+link(['unsupported-contrast','forced-template','repetition'])+'. Vor einem Änderungsvorschlag wird geprüft, ob die Formulierung dem Anliegen der Leser dient.',status:'Der Katalog enthält bedingte Regeln für Überschriften, abgeleitet aus Furzes Beispielen und der Forschung zu Textüberarbeitung: das Thema des Abschnitts benennen, hilfreiche Fragen und Bilder erhalten und keine Ergebnisse ankündigen, die im Abschnitt fehlen. Wiederholte rhetorische Muster auf der ganzen Seite erfordern weiterhin eine Kontextanalyse durch die einbindende Anwendung.'},
  studies:{title:'Analysen, die sich aus den Studien ableiten',body:'Studien zur Textüberarbeitung fließen in '+link(['word-choice','format-fit','task-relevance'])+' ein: ungelenke und rein schmückende Formulierungen sowie fehlende nützliche Details prüfen. Forschung zu Behauptungen und übermäßiger Zustimmung betrifft '+link(['claims-evidence','calibrated-uncertainty'])+': ursprüngliche und vorgeschlagene Aussagen mit den bereitgestellten Belegen vergleichen.',status:'Diese Erkenntnisse sind als Anweisungen für kontextbezogene Prompts umgesetzt. Die statistischen Modelle der Studien, semantische Prüfungen auf Aussageerhalt und kalibrierte Bewertungsmodelle sind nicht implementiert. Forschung zu Wortschatzverschiebungen und Urheberschaft begrenzt die möglichen Schlüsse: Ein Worttreffer belegt keine Urheberschaft.'},
  strategies:{title:'Wie die Erkenntnisse in Prompts einfließen',body:'Jede ausgewählte Regel trägt ihr Erkennungsmerkmal, die Auswirkung auf Leser, eine Bearbeitungsanweisung, ein bedingtes Vorher-nachher-Beispiel und ein Beispiel zum Beibehalten bei. Die Prompt-Zusammenstellung ergänzt Zielgruppe und Ziel, Grenzen des Quellkontexts, begründete Alternativen und Vorgaben zur Prüfungsabdeckung.',status:'Die einbindende Anwendung führt den Prompt aus. Der Qualitätsbenchmark bewertet bereitgestellte Ergebnisse anhand unveränderlicher Eingaben und gesonderter menschlicher Urteile. Das Paket trainiert keine Modelle, führt keine Schleife zur Selbstüberarbeitung aus und wertet die Zustimmung eines Modells nicht als unabhängige Prüfung.'},
  libraries:{title:'Adapter für zusätzliche statische Analysen',body:'Vale eignet sich für in Git gepflegte Begriffs- und Stilregeln, textlint für Regeln, die die Markup-Struktur berücksichtigen. LanguageTool kommt für deutsche und englische Grammatikprüfungen infrage. Harper bietet lokale englische Korrekturprüfungen im Browser. Die Einträge unten beschreiben weitere Möglichkeiten und ihre jeweiligen Grenzen.',status:'Keines der aufgeführten Werkzeuge ist bisher mitgeliefert oder angebunden. Ein Adapter muss Werkzeug und Version, Quelltext und verifizierte Textbereiche erhalten. Nach Prüfung durch die einbindende Anwendung lassen sich die Befunde mit @voice/review anzeigen. Ob eine Überschrift im Kontext funktioniert und ob Aussagen belegt sind, muss gesondert geprüft werden.'}
 }:{
  practice:{title:'Rules from these observations',body:'Unrequested interpretation informs '+link(['meta-framing','reader-goal'])+'. Formulaic contrasts and repeated rhetorical structures inform '+link(['unsupported-contrast','forced-template','repetition'])+'. Review the wording against the reader’s task before proposing a change.',status:'The catalogue now includes conditional heading guidance derived from Furze’s examples and the editing research: identify the section’s subject, preserve useful questions and imagery, and avoid implying results the section does not contain. Full-page rhetorical repetition still requires a host-run contextual review.'},
  studies:{title:'Analyses supported by the studies',body:'Editing studies inform '+link(['word-choice','format-fit','task-relevance'])+': inspect awkward phrasing, decorative wording and missing useful detail. Studies of claims and agreement inform '+link(['claims-evidence','calibrated-uncertainty'])+': compare the original and proposed claims against the supplied evidence.',status:'These are contextual prompt instructions. The package does not implement the papers’ statistical models, semantic fidelity checks or calibrated judges. Lexical drift and authorship studies constrain interpretation; they do not turn a word match into an authorship finding.'},
  strategies:{title:'How the prompts use this evidence',body:'Each selected rule contributes its symptom, reader impact, editing instruction, conditional before/after example and an acceptable keep example. The composer adds audience and goal, source boundaries, justified alternatives and coverage requirements.',status:'The host runs the prompt. The quality benchmark evaluates supplied results against frozen inputs and separate reviewer judgments. The package does not fine-tune models, run a self-revision loop or treat a model’s approval as independent verification.'},
  libraries:{title:'Adapters for additional static analysis',body:'Vale can supply Git-owned terminology and style rules; textlint can supply markup-aware rule execution. LanguageTool is a candidate for English/German grammar. Harper supplies English browser-local proofreading. The records below describe other options and their exact limits.',status:'No listed engine is bundled or connected yet. An adapter must retain engine/version, source text and verified spans. Engine findings can be displayed with @voice/review after host validation; contextual heading quality and factual support still require separate review.'}
 };
 const item=content[group];
 if(!item)throw new Error('Unknown research application group: '+group);
 return '<div class="research-choice"><h3>'+item.title+'</h3><p>'+item.body+'</p><p><strong>'+(de?'Umsetzung:':'Implementation:')+'</strong> '+item.status+'</p><a href="#'+(de?'de-':'')+'library-analysis">'+(de?'Den genauen Analyseumfang der Library ansehen':'See the Library’s exact analysis coverage')+'</a></div>';
}
export function libraryAnalysis(locale='en'){
 const de=locale==='de',t=(en,dt)=>de?dt:en,prefix=de?'de-':'';
 const scanned=new Set(patterns.map(pattern=>pattern.ruleId));
 const rows=catalogue.rules.map(rule=>'<tr><th scope="row">'+ruleLink(rule.id,de?'de':'en')+'</th><td>'+e(rule.antiPattern.name[de?'de':'en'])+'</td><td>'+(scanned.has(rule.id)?t('Fixed phrase candidates + contextual prompt','Feste Formulierungsmuster + kontextbezogener Prompt'):t('Contextual prompt','Kontextbezogener Prompt'))+'</td></tr>').join('');
 return `<section id="${prefix}library-analysis"><h2>${t('Writing tools and HTML visualization','Schreibwerkzeuge und HTML-Darstellung')}</h2>
<div class="research-table-wrap" tabindex="0" role="region" aria-label="${t('Two Voice libraries','Die beiden Voice-Libraries')}"><table class="research-library-table"><thead><tr><th>${t('Package','Paket')}</th><th>${t('Responsibility','Aufgabe')}</th><th>${t('Result','Ergebnis')}</th></tr></thead><tbody>
<tr><th><code>@voice/writing-rules</code></th><td>${t('Rule knowledge, prompt composition, static cues and a provider-neutral LLM tool interface.','Regelwissen, Prompt-Zusammenstellung, statische Hinweise und eine anbieterunabhängige Werkzeugschnittstelle für LLMs.')}</td><td>${t('Inspectable instructions, exact candidate spans and host-supplied model evidence.','Einsehbare Anweisungen, genaue Textbereiche der Prüfkandidaten und von der Anwendung bereitgestellte Vergleichsdaten zu Modellen.')}</td></tr>
<tr><th><code>@voice/review</code></th><td>${t('Render host-supplied findings and feedback in HTML; handle text selection and source mapping.','Befunde und Feedback der einbindenden Anwendung in HTML anzeigen; Textauswahl und Quellzuordnung verarbeiten.')}</td><td>${t('Annotations and callbacks for your review interface.','Markierungen und Callbacks für die eigene Review-Oberfläche.')}</td></tr>
</tbody></table></div><p>${t('The host connects these packages: it supplies source context and available models, executes an admitted model call, validates its findings and persists decisions. Studio provides a local application around the review workflow.','Die einbindende Anwendung verbindet die Pakete: Sie stellt Quellkontext und verfügbare Modelle bereit, führt einen freigegebenen Modellaufruf aus, prüft die Befunde und speichert Entscheidungen. Studio bietet dafür eine lokale Anwendung mit Review-Ablauf.')}</p>
<h3>${t('What the static scanner does','Was der statische Scanner prüft')}</h3><p>${t('<code>findWritingSignals</code> runs '+patterns.length+' fixed English/German regular expressions across raw text, covering '+scanned.size+' of '+catalogue.rules.length+' rules. It returns the matched quote, UTF-16 start/end positions, rule ID, covered and unscanned rules, and truncation status. For example, it can locate <code>it is worth noting</code> or <code>könnte möglicherweise</code>.','<code>findWritingSignals</code> durchsucht den Rohtext mit '+patterns.length+' festen deutschen und englischen regulären Ausdrücken. Damit deckt die Funktion '+scanned.size+' von '+catalogue.rules.length+' Regeln ab. Sie liefert das gefundene Zitat, Anfang und Ende als UTF-16-Positionen, die Regel-ID, geprüfte und nicht gescannte Regeln sowie einen Hinweis auf gekürzte Ergebnisse. Beispielsweise erkennt sie <code>it is worth noting</code> oder <code>könnte möglicherweise</code>.')}</p>
<p>${t('The scanner does not parse sentences or markup, exclude code/quotations, interpret negation, check facts or assess the purpose of a heading. <code>not guaranteed</code> can still produce a candidate. The rejected heading <code>Which model and prompt earn their cost?</code> produced zero local signals: its problem requires context.','Der Scanner analysiert keine Satz- oder Markup-Struktur. Er schließt Code und Zitate nicht aus, deutet keine Verneinungen, prüft keine Fakten und bewertet nicht den Zweck einer Überschrift. Deshalb kann auch <code>not guaranteed</code> einen Prüfkandidaten erzeugen. Die beanstandete Überschrift <code>Which model and prompt earn their cost?</code> ergab keinen lokalen Treffer. Ihr Problem lässt sich erst im Kontext beurteilen.')}</p>
<details><summary>${t('All '+catalogue.rules.length+' rules and their analysis method','Alle '+catalogue.rules.length+' Regeln mit ihrer Analysemethode')}</summary><div class="research-table-wrap" tabindex="0" role="region" aria-label="${t('Writing rule implementation coverage','Implementierte Prüfungen je Schreibregel')}"><table class="research-library-table"><thead><tr><th>${t('Rule','Regel')}</th><th>${t('Review concern','Prüfgegenstand')}</th><th>${t('Implemented method','Implementierte Methode')}</th></tr></thead><tbody>${rows}</tbody></table></div></details>
<h3>${t('What enters the review prompt','Was der Review-Prompt enthält')}</h3><p>${t('<code>composeWritingReviewPrompt</code> includes the selected rule instructions, examples and keep cases. Rule evidence links and the complete exception list remain available through the catalogue and the rule tool. Research records become package behavior only through a reviewed catalogue or analyzer change and a new build.','<code>composeWritingReviewPrompt</code> enthält die ausgewählten Regelanweisungen, Beispiele und Fälle, in denen der Text bleiben soll. Quellenlinks zu den Regeln und die vollständigen Ausnahmelisten sind über den Katalog und das Regelwerkzeug verfügbar. Forschungseinträge verändern das Verhalten des Pakets erst nach einer geprüften Änderung am Katalog oder Analysecode und einem neuen Build.')}</p>
<p>${t('Catalogue <code>'+e(catalogue.version)+'</code> incorporates the heading-feedback lesson into wording, format and framing guidance. Broader analyses—cross-page repetition, claim preservation, alternative diversity and heading-to-content alignment—remain contextual review tasks, with results to test against independently reviewed examples.','Katalog <code>'+e(catalogue.version)+'</code> enthält Hinweise zur Wortwahl, Form und Einordnung von Überschriften. Weitergehende Analysen benötigen ein kontextbezogenes Review: Wiederholungen über mehrere Seiten, Erhalt von Aussagen, Unterschiede zwischen Alternativen und die Übereinstimmung von Überschrift und Inhalt. Ihre Ergebnisse müssen an unabhängig bewerteten Beispielen geprüft werden.')}</p>
<p><a href="../guides/writing-tools/">${t('Use Voice through LLM tool calls','Voice über LLM-Werkzeugaufrufe einbinden')}</a> · <a href="../guides/review-benchmarks/">${t('Reproduce a quality evaluation','Eine Qualitätsbewertung reproduzieren')}</a> · <a href="../library/">${t('Use the two packages','Die beiden Pakete verwenden')}</a></p></section>`;
}
