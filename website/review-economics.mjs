import data from './research/review-economics.json' with {type:'json'};
import {escape as e} from './template.mjs';
const number=value=>new Intl.NumberFormat('en-US').format(value);
const percent=value=>(value*100).toFixed(1)+'%';
function renderEconomicsSource(source) {
 return '<details id="'+e(source.id)+'"><summary>'+e(source.title)+'<span> · '+e(source.kind)+'</span></summary><p>'+e(source.finding)+'</p><p><strong>Limits:</strong> '+e(source.limits)+'</p><p><strong>Use with Voice — our assessment:</strong> '+source.apiUse.availableNow.map(symbol=>'<a href="../guides/writing-rules/"><code>'+e(symbol)+'</code></a>').join(' and ')+': '+e(source.apiUse.currentUse)+'</p><p><strong>Proposed API work:</strong> '+e(source.apiUse.proposed)+'</p><p><a href="'+e(source.url)+'">Read the primary source</a> · '+e(source.read)+'</p></details>';
}
export function reviewEconomics(){
 const evidence=data.organizationResearch.sources.map(renderEconomicsSource).join('');
 const measured=data.promptMeasurement,scan=data.surfaceEvaluation,models=data.modelPricing;
 const rows=[...measured.sameCoverageComparisons].reverse().map(comparison=>{
  const separate=measured.summaries.find(s=>s.id===comparison.separate),bundle=measured.summaries.find(s=>s.id===comparison.together);
  return `<tr><th scope="row">${separate.ruleCountPerFixture} rules</th><td>${number(separate.totalRequests)} / ${number(bundle.totalRequests)}</td><td>${number(separate.totalPayload.utf8Bytes)}</td><td>${number(bundle.totalPayload.utf8Bytes)}</td><td>${percent(comparison.payloadUtf8ReductionFraction)}</td></tr>`;
 }).join('');
 const modelRows=models.candidates.map(model=>{const source=models.sources.find(s=>s.id===model.sourceIds.find(id=>id.includes('pricing')));return `<tr><th scope="row"><code>${e(model.modelId)}</code></th><td>$${model.pricing.standard.input.toFixed(2)}</td><td>$${model.pricing.standard.output.toFixed(2)}</td><td><a href="${e(source.url)}">Official pricing</a></td></tr>`}).join('');
 return `<section id="economics"><h2>Which model and prompt earn their cost?</h2><p>Evaluate the model and the prompt together. A cheaper model can be sufficient for a focused review; a larger bundle can remove repeated context but miss a subtle problem. Voice compares useful findings, preserved intent, complete coverage, latency and total cost.</p><h3>Compare prompt organizations</h3><div class="research-table-wrap" tabindex="0" role="region" aria-label="Prompt organization experiments"><table class="research-library-table"><thead><tr><th scope="col">Organization</th><th scope="col">Question to test</th><th scope="col">Fair comparison</th></tr></thead><tbody><tr><th scope="row">One rule per request</th><td>Does narrow focus recover problems a bundle misses?</td><td>Count every request and deduplicate findings for the complete review.</td></tr><tr><th scope="row">Two related rule groups</th><td>Does grouping retain focus while sharing context?</td><td>Use the same six rules, split into two fixed groups.</td></tr><tr><th scope="row">One six-rule profile</th><td>Can one compact review preserve the same useful findings?</td><td>Use the same cases, rules and total finding allowance.</td></tr><tr><th scope="row">All twenty rules</th><td>Does broader review justify extra instructions and output?</td><td>Compare twenty rules separately and together in their own cohort.</td></tr><tr><th scope="row">Cheaper review, selective escalation</th><td>When does a second model improve the result enough to pay for both?</td><td>Define escalation triggers and audit cases that were not escalated.</td></tr></tbody></table></div><p>The first four organizations have a local experiment preparer. Selective escalation is a later experiment. None has model-quality results yet.</p><details id="prompt-layout"><summary>How to assemble a review prompt</summary><p>Keep the rule set, response contract and valid keep examples stable within an experiment. Supply the reader goal, relevant facts, accepted decisions and versioned source separately. Ask for exact quotes, rule IDs and concise reasons; generate alternatives only for justified changes.</p><pre><code>Review instructions + selected rules + keep examples
+ audience and reader goal
+ versioned source, relevant facts and retained decisions
→ findings, coverage and justified keep/change choices</code></pre><p><code>composeWritingReviewPrompt</code> currently puts audience and goal before the rule text; the host supplies the document separately. A reusable cached rubric prefix requires a different serialization and its own comparison. Long rule sets, compressed examples and reordered context can change what a model notices. Measure those changes rather than assuming that fewer tokens preserve quality.</p></details><h3>Our first local experiment</h3><p>We constructed requests with the real Library for ${scan.fixtureCount} authored English/German cases. Each comparison uses the same rules, source, audience and goal. The requests were measured locally; none was sent to a model.</p><div class="research-table-wrap" tabindex="0" role="region" aria-label="Equal-coverage prompt input comparison"><table class="research-library-table"><thead><tr><th scope="col">Same scope</th><th scope="col">Separate / bundled requests</th><th scope="col">Separate UTF-8 bytes</th><th scope="col">Bundled UTF-8 bytes</th><th scope="col">Input reduction</th></tr></thead><tbody>${rows}</tbody></table></div><p><strong>Bytes are not tokens.</strong> No tokenizer was available for this run. The measurement excludes provider message framing, cache behavior, output, reasoning, retries and latency. Separate requests also allow more findings in total under the same per-request limit. These results establish reduced repeated input, not equivalent review quality or a percentage cost saving.</p><p>The same fixtures produced ${scan.candidateCount} local scanner candidates: ${scan.candidatesAgreeingWithEditorialLabel} agreed with the authored review labels and ${scan.candidatesNotAgreeingWithEditorialLabel} occurred on deliberate keep cases. All ${scan.semanticProblemCasesWithNoCandidate} semantic problem cases lacked a candidate. These examples demonstrate why local signals need context; they do not estimate performance on a representative corpus.</p><p><a href="../sources/research/review-economics.json">Open measurements, source hashes and pricing JSON</a></p><h3>Compare inexpensive reviewers with a stronger reference</h3><p>These candidates were selected from official API documentation. Their ability to recognize Voice's patterns has not been measured. Rates below are USD per million ordinary input or billed output tokens, checked ${e(models.checkedAt)}, for standard short-context text requests.</p><div class="research-table-wrap" tabindex="0" role="region" aria-label="Model candidate standard token rates"><table class="research-library-table"><thead><tr><th scope="col">Model</th><th scope="col">Input / 1M tokens</th><th scope="col">Output / 1M tokens</th><th scope="col">Source</th></tr></thead><tbody>${modelRows}</tbody></table></div><p>Test Luna, Haiku and Flash-Lite as efficiency candidates on the same cases and rule scope. Astra provides a stronger-tier comparison; human review still resolves disagreements. Record exact model versions and settings, including reasoning effort. English results do not establish German performance.</p><h3>Choose by cost per accepted review</h3><p>Divide the total cost of all attempts by the number of complete reviews that pass the declared quality criteria. Include retries, rejected outputs and both stages of an escalation. Keep missed issues, false changes on acceptable text, fact preservation and latency visible alongside that figure. An empty result is useful only when the requested scope was reviewed and the text warrants keeping.</p><p>Cost per accepted finding is a secondary measure: rewarding more findings can encourage unnecessary edits. Choose a configuration only after its quality results justify the saving. Current prices define candidates to test, not a measured winner.</p><details class="research-entry" id="cost-accounting"><summary>Cache, batching and output costs<span>What a token price table leaves out</span></summary><div class="research-detail"><p>Cache reuse needs a matching eligible prefix. Write charges, read charges and retention differ between providers; explicit Gemini caches also incur storage cost. The pricing snapshot records these conditions and an unresolved Google Batch cache-rate discrepancy. <a href="https://developers.openai.com/api/docs/guides/prompt-caching">OpenAI caching</a>; <a href="https://platform.claude.com/docs/en/build-with-claude/prompt-caching">Claude caching</a>; <a href="https://ai.google.dev/gemini-api/docs/caching">Gemini caching</a>.</p><p>Combining rules in one prompt differs from a provider's asynchronous Batch API. Batch discounts can suit offline benchmarks; completion windows make them a separate choice from an interactive review. <a href="https://developers.openai.com/api/docs/guides/batch">OpenAI Batch</a>; <a href="https://platform.claude.com/docs/en/build-with-claude/batch-processing">Claude Batch</a>; <a href="https://ai.google.dev/gemini-api/docs/batch-api">Gemini Batch</a>.</p><p>The host should record actual ordinary input, cache writes, cache reads and billed output or reasoning separately. Report cost per accepted complete review, with useful findings, latency and errors as separate measures. A smaller visible answer does not by itself prove a lower bill.</p></div></details><h3>What research supports</h3><p><strong>Rule bundling</strong> shares one passage across checks. <strong>Sample packing</strong> shares one rubric across passages. A provider’s asynchronous <strong>Batch API</strong> schedules separate requests. The studies below test sample packing or context use; their results motivate our experiments without establishing the best Voice configuration.</p>${evidence}<h3>Use this with the current API</h3><p>Use <code>selectWritingRules</code> to fix the review scope and <code>composeWritingReviewPrompt</code> to build comparable requests. <code>findWritingSignals</code> supplies local candidate spans; an empty result must not skip requested semantic review. Model execution, budget enforcement and output validation currently belong to the host.</p><p><a href="../guides/writing-rules/">Use the current Library functions</a> · <a href="../writing-patterns/#en-playground">Compose a profile prompt locally</a></p></section>`;
}
