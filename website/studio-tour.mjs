export const studioImages = ['studio-live-review', 'studio-source', 'studio-focus'];

export function studioTour(locale) {
  const de=locale==='de';
  const full=de?'Aufnahme in voller Größe öffnen':'Open full-size screenshot';
  const shot=(name,title,description,width,height,classes='',eager=false)=>`<figure class="studio-shot ${classes}"><a href="../assets/${name}.png" target="_blank" rel="noopener" aria-label="${full}: ${title}"><img src="../assets/${name}.png" alt="${description}" width="${width}" height="${height}" loading="${eager?'eager':'lazy'}"></a><figcaption><h3>${title}</h3><p>${description}</p><a href="../assets/${name}.png" target="_blank" rel="noopener">${full} ↗</a></figcaption></figure>`;
  return `<section class="studio-tour" aria-label="${de?'Einblicke in Voice Studio':'Inside Voice Studio'}"><div class="studio-tour-heading"><h2>${de?'Von der Seite bis zur Quelle.':'From the page to its source.'}</h2><p>${de?'Website, Quelldatei und mobiler Review-Bereich am Beispiel der Agent-Studio-Website.':'Website, source file and mobile review area, using the Agent Studio website.'}</p></div>${shot('studio-live-review',de?'Im Kontext entscheiden':'Decide in context',de?'Die Website bleibt sichtbar. Die ausgewählte Passage und ihre Review-Optionen stehen direkt daneben.':'The website stays visible, with the selected passage and its review options alongside it.',1180,780,'',true)}<div class="studio-shot-details">${shot('studio-source',de?'Die Originaldatei nachvollziehen':'Inspect the original file',de?'Die Quellansicht zeigt das TypeScript-Original und den relativen Dateipfad. Der Git-Bezug kennzeichnet die aktuelle Arbeitskopie.':'The source view shows the original TypeScript and its relative file path. Git context identifies the current working copy.',1180,780)}${shot('studio-focus',de?'Auch auf kleinem Bildschirm prüfen':'Review on a small screen',de?'Im Website-Fokus erhält die Seite den verfügbaren Platz. Navigation und Review lassen sich bei Bedarf aufklappen.':'Website focus gives the page the available space. Open navigation or review when you need it.',430,860,'studio-shot-mobile')}</div><p class="studio-shot-note">${de?'Echte lokale Aufnahmen · Englische Studio-Oberfläche · Zum Vergrößern ein Bild öffnen.':'Actual local captures · English Studio interface · Open an image to inspect it at full size.'}</p></section>`;
}
