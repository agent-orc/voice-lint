node_modules/
dist/
coverage/
*.log

# Local diagnostics may contain text-derived data.
.voice-lint/cache/
.voice-lint/diagnostics/
.voice-lint/*.token

# Runtime authority and credentials belong outside repositories. These patterns
# are defense in depth for accidental local files, not a supported project layout.
.env
.env.*
!.env.example
.voice-lint/runtime.yml
.voice-lint/runtime.yaml

# Studio builds and private local state.
node_modules/
**/dist/
**/bin/
**/obj/
.voice-studio/
test-results/
examples/**/.voice-lint/
.angular/
frontend/.angular/
*.log
.voice-runtime/
# Local maintenance history and private investigation residue.
.local/
